# Smoke Benchmark

Tiny local benchmark used to verify that the repository can load a corpus, index segments, run questions, calculate deterministic metrics, and build a report without cloud services.

